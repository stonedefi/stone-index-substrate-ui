// TODO: Need to mock the ApiPromise, WsProvider,web3Accounts,web3Enable,keyring...object and etc.
// So won’t deal with it temporarily due to time.

describe('Transaction Test Suite', () => {
  it('just display', () => {
    expect(1).toBe(1);
  })
});